name: Skript-LuckPerms
description: A basic plugin for using the LuckPerms API in Skript.
prefix: Skript-LuckPerms
version: '1.0-BETA'
load: POSTWORLD
main: owlbe.skriptLuckPerms.SkriptLuckPerms
api-version: '1.21.11'

authors: [ OwlBe ]

dependencies:
  server:
    LuckPerms:
      load: BEFORE
      required: true
    Skript:
      load: BEFORE
      required: true